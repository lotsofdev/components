<a href="{{ $link->href }}" id="{{ $id }}" class="button -{{ $style }} {{ $class }}" title="{{ $link->title }}"
    target="{{ $link->target }}" aria-label="{{ $link->ariaLabel }}" rel="{{ $link->rel }}">
    {{ $link->text }}
</a>